[project]
name = "example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-interface",
]

[tool.uv.sources]
python-interface = { path = "../python_interface", editable = true }
